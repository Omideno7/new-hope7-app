# New Hope 7 — نسخه 2.3.0

این نسخه فقط قابلیت‌های زیر را اضافه می‌کند و فایل اصلی `js/app.js` را تغییر نمی‌دهد:

1. جلوگیری از دسترسی مهمان به موعظه‌های صوتی، کتاب مقدس صوتی، کتاب‌ها و جزوه‌ها.
2. بررسی واقعی حساب و تأیید مدرسه در سرور.
3. خصوصی‌کردن فایل‌های صوتی و ساخت لینک موقت ۱۵ دقیقه‌ای فقط برای کاربر مجاز.
4. اتصال کد خادمان به حساب همان شخص؛ بعد از یک‌بار ورود کد، دسترسی حسابی ذخیره می‌شود.
5. رفع خروج از یادداشت آیه هنگام زدن Space.
6. انتخاب گروهی آیات، ذخیره گروهی، هایلایت رنگی، یادداشت مشترک، کپی و اشتراک‌گذاری.
7. جلوگیری از ذخیره عمومی کاتالوگ‌ها و لینک‌های محافظت‌شده در Service Worker.

## فایل‌های تغییرکرده/جدید

### فایل‌های وب — در GitHub آپلود شوند

- `index.html` — جایگزین فایل قبلی
- `service-worker.js` — جایگزین فایل قبلی
- `css/v2.3.0-access-bible.css` — فایل جدید
- `js/nh7-access-bootstrap-v230.js` — فایل جدید
- `js/nh7-app-enhancements-v230.js` — فایل جدید

### Supabase Edge Functions

- `supabase/functions/nh7-content-access/index.ts` — Function جدید با نام `nh7-content-access`
- `supabase/functions/nh7-library-access/index.ts` — جایگزین کامل Function فعلی `nh7-library-access`

برای هر دو Function، گزینه **Verify JWT** روشن باشد. برنامه JWT کاربر را در Header ارسال می‌کند و خود Function نیز اعتبار حساب را دوباره بررسی می‌کند.

Secrets لازم:

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

### SQL — فقط در آخر اجرا شود

- `supabase/migrations/20260727_v230_protected_content_bible_batch.sql`

این SQL فایل‌های `church-audio` را خصوصی می‌کند و دسترسی عمومی قدیمی را می‌بندد. بنابراین قبل از اجرای SQL حتماً هر دو Edge Function و پنج فایل وب بالا باید نصب شده باشند.

## ترتیب دقیق نصب

1. ابتدا از پروژه فعلی Backup بگیرید.
2. دو Edge Function را Deploy کنید.
3. دو فایل JavaScript جدید و فایل CSS جدید را در مسیر درست GitHub قرار دهید.
4. `index.html` و `service-worker.js` را جایگزین کنید.
5. صبر کنید GitHub Pages نسخه جدید را منتشر کند.
6. صفحه اپ را با یک تب Private باز کنید و ورود/ثبت‌نام، کتاب مقدس و ابزار انتخاب گروهی را تست کنید.
7. فقط پس از موفقیت تست وب، فایل SQL را یک‌بار در Supabase SQL Editor اجرا کنید.
8. دوباره این موارد را تست کنید:
   - مهمان نتواند Audio Messages، Audio Bible و Library را باز کند.
   - کاربر واردشده ولی تأییدنشده پیام ثبت‌نام/تأیید ببیند.
   - کاربر تأییدشده فایل صوتی را پخش کند.
   - جزوه خادمان ابتدا کد بخواهد و بعد از تأیید، برای همان حساب باز شود.
   - Space در یادداشت آیه عادی کار کند.
   - چند آیه با هم انتخاب، ذخیره، رنگی و یادداشت‌گذاری شوند.
9. بعد از تست PWA، نسخه Android/Google Play و سپس iOS/App Store ساخته شود.

## نکته امنیتی مهم

تا زمانی که SQL اجرا نشده، فایل‌های قدیمی `church-audio` هنوز با URL عمومی قبلی قابل دسترسی‌اند. اجرای SQL این مسیر عمومی را می‌بندد، اما باید حتماً بعد از Deploy شدن Edge Function و فایل‌های وب انجام شود تا کاربران مجاز قطع دسترسی نشوند.

## برگشت اضطراری

در صورت بروز مشکل قبل از اجرای SQL، فقط `index.html` و `service-worker.js` قبلی را برگردانید.

در صورت بروز مشکل بعد از اجرای SQL، فایل‌های صوتی را عمومی نکنید. ابتدا Edge Function و Header احراز هویت را بررسی کنید؛ عمومی‌کردن مجدد Bucket امنیت محتوا را از بین می‌برد.
