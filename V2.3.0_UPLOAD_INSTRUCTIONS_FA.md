# New Hope 7 — نسخه 2.3.0

این بسته فقط فایل‌های تغییرکرده و فایل‌های جدید را دارد. فایل اصلی `js/app.js` تغییر نکرده است.

## قابلیت‌های این نسخه

1. مهمان و کاربر تأییدنشده نمی‌تواند موعظه‌های صوتی، کتاب مقدس صوتی، کتاب‌ها و جزوه‌ها را باز کند.
2. دسترسی واقعی با حساب کاربری و تأیید مدرسه در سرور بررسی می‌شود.
3. فایل‌های صوتی خصوصی می‌شوند و فقط برای کاربر مجاز لینک موقت ساخته می‌شود.
4. کد خادمان بعد از یک بار استفاده به حساب همان شخص متصل می‌شود و ادمین می‌تواند آن را لغو کند.
5. زدن Space در یادداشت آیه دیگر پنجره را نمی‌بندد.
6. چند آیه را می‌توان هم‌زمان انتخاب، ذخیره، هایلایت، یادداشت‌گذاری، کپی و اشتراک‌گذاری کرد.
7. کاتالوگ‌ها و لینک‌های محافظت‌شده در Cache عمومی ذخیره نمی‌شوند.
8. پنل دانشجو از Patchهای متداخل قبلی پاک شده و فقط یک اسکرول، یک دکمه بستن و یک Loader تحلیل فعالیت دارد.

## فایل‌های وب که در GitHub آپلود می‌شوند

### فایل‌های جایگزین

- `index.html`
- `admin.html`
- `service-worker.js`

### فایل‌های جدید

- `css/v2.3.0-access-bible.css`
- `css/admin-v2.3.0-student-profile.css`
- `js/nh7-access-bootstrap-v230.js`
- `js/nh7-app-enhancements-v230.js`
- `js/admin-v2.2.5-v230.js`

فایل‌های قدیمی آزمایشی مانند `admin-stage1-test.html`، `admin-stage1-clean-v240.html` و اسکریپت‌های `admin-stage1-*` را برای این نسخه استفاده نکنید. فایل `admin.html` جدید مستقیماً نسخه تمیز `js/admin-v2.2.5-v230.js` را بارگذاری می‌کند.

## Supabase Edge Functions

- `supabase/functions/nh7-content-access/index.ts` — Function جدید با نام `nh7-content-access`
- `supabase/functions/nh7-library-access/index.ts` — جایگزین کامل Function فعلی `nh7-library-access`

برای هر دو Function گزینه **Verify JWT** روشن باشد. برنامه JWT حساب کاربر را در Header می‌فرستد و Function نیز اعتبار حساب را دوباره بررسی می‌کند.

Secrets لازم:

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

## SQL — فقط در آخر اجرا شود

- `supabase/migrations/20260727_v230_protected_content_bible_batch.sql`

این SQL دسترسی عمومی قدیمی را می‌بندد و Bucket صوتی `church-audio` را خصوصی می‌کند. پیش از اجرای SQL باید هر دو Edge Function و تمام فایل‌های وب بالا نصب شده باشند.

## ترتیب دقیق نصب

1. از پروژه فعلی Backup بگیرید.
2. Function جدید `nh7-content-access` را Deploy کنید.
3. Function فعلی `nh7-library-access` را با فایل جدید جایگزین و Deploy کنید.
4. فایل‌های جدید CSS و JavaScript را با همان پوشه‌بندی داخل GitHub آپلود کنید.
5. `index.html`، `admin.html` و `service-worker.js` را جایگزین کنید.
6. منتظر پایان انتشار GitHub Pages بمانید.
7. قبل از SQL، در یک تب Private این موارد را آزمایش کنید:
   - اپ و پنل ادمین باز شوند.
   - کتاب مقدس متنی و بخش‌های سالم قبلی کار کنند.
   - Space داخل یادداشت آیه عادی فاصله ایجاد کند.
   - انتخاب گروهی آیات و نوار ابزار آن ظاهر شود.
   - پروفایل دانشجو فقط داخل خودش اسکرول شود.
   - صفحه پشت پروفایل حرکت نکند.
   - ضربدر قرمز پروفایل را ببندد.
   - بخش «رصد دقیق فعالیت دانشجو» داخل پروفایل دیده شود؛ حتی اگر داده‌ای ثبت نشده باشد.
8. فقط بعد از موفقیت تست مرحله ۷، SQL را یک بار در Supabase SQL Editor اجرا کنید.
9. بعد از SQL این موارد را دوباره آزمایش کنید:
   - مهمان نتواند Audio Messages، Audio Bible و Library را باز کند.
   - کاربر واردشده ولی تأییدنشده پیام ثبت‌نام/تأیید ببیند.
   - کاربر تأییدشده فایل صوتی را پخش کند.
   - جزوه خادمان ابتدا کد بخواهد و پس از تأیید برای همان حساب باز شود.
   - همان کاربر در ورود بعدی بدون واردکردن دوباره کد، تا زمان اعتبار Grant دسترسی داشته باشد.
   - چند آیه با هم ذخیره، رنگی و یادداشت‌گذاری شوند.
10. بعد از تأیید PWA، نسخه Android/Google Play و سپس نسخه iOS/App Store ساخته شود.

## نکته امنیتی مهم

تا پیش از اجرای SQL، URLهای قدیمی `church-audio` هنوز عمومی هستند. پس از اجرای SQL فقط Edge Function لینک موقت تولید می‌کند. SQL را زودتر از فایل‌های وب و Edge Function اجرا نکنید، چون کاربران مجاز هم موقتاً قطع دسترسی خواهند شد.

## برگشت اضطراری

قبل از SQL: `index.html`، `admin.html` و `service-worker.js` قبلی را برگردانید.

بعد از SQL: Bucket را دوباره عمومی نکنید. ابتدا Deploy بودن Functionها، روشن بودن Verify JWT، Secrets و ارسال Authorization Header را بررسی کنید. عمومی‌کردن Bucket امنیت محتوا را از بین می‌برد.
