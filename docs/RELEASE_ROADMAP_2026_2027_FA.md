# نقشهٔ راه امن انتشار New Hope 7 — ۲۰۲۶ تا ۲۰۲۷

آخرین نسخهٔ سالم مبنا: `main 2.3.9.49`

اصل قطعی این برنامه:

- هیچ شاخهٔ آزمایشی به‌طور کامل جایگزین `main` نمی‌شود.
- هر قابلیت در یک موج کوچک و جدا وارد می‌شود.
- پیش از Merge، Diff، حذف فایل‌ها، Version، Service Worker و ماژول‌های حساس بررسی می‌شوند.
- Merge فقط پس از تست واقعی روی گوشی و تأیید کاربر انجام می‌شود.
- پس از هر Merge یک نقطهٔ بازگشت سالم نگهداری می‌شود.

## موج ۱A — اپوکریفا

شاخه: `release/wave1a-apocrypha-23949`

دامنهٔ تغییر مجاز:

- ۱۹ کتاب اپوکریفا به فارسی، انگلیسی و کرواتی.
- فقط ترجمه‌های تازهٔ پروژه که بر اساس متن انگلیسی آماده و تأیید شده‌اند.
- نمایش پیوستهٔ آیات مانند کتاب‌مقدس.
- بزرگ و کوچک‌کردن متن.
- هایلایت چندرنگ، یادداشت، ذخیره/حذف ذخیره، کپی و اشتراک‌گذاری.
- رفتن مستقیم از آیهٔ ذخیره‌شده به همان کتاب، باب و آیه.
- بارگذاری مرحله‌ای برای جلوگیری از مصرف زیاد حافظه.

بخش‌های ممنوع از تغییر در این موج:

- پلیر صوتی و دانلود آفلاین.
- مدرسه، تکلیف و امتحان.
- ثبت‌نام، ورود و بازیابی رمز.
- ویدیو و کد دسترسی.
- پنل ادمین.
- Settings و حذف حساب.

شرط Merge:

1. هر ۱۹ کتاب در FA/EN/HR باز شوند.
2. اول، وسط و آخر هر کتاب نمونه‌برداری شود.
3. مکابیان چهارم به‌طور ویژه بررسی شود.
4. ابزارهای آیه و Saved Verse روی iPhone و Android کار کنند.
5. پلیر، مدرسه و امتحان بدون Regression باقی بمانند.
6. Validation سبز باشد.

## موج ۱B — پلن‌های روحانی

شاخهٔ جدا پس از تثبیت موج ۱A ساخته می‌شود.

دامنه:

- ۳۰ روز دعا در روح و به زبان‌ها.
- زندگی در فیض.
- روزه و دعا با تعلیم انواع روزه.
- اطاعت و برکت.
- نجات و زندگی تازه در مسیح.
- تغییر طرز تفکر، فقط پس از تکمیل و بازبینی نسخهٔ ۳۰ روزه.
- حفظ Progress و نمایش آیات به‌صورت درون‌برنامه‌ای.

## موج ۲ — کتابخانه و ۹ کتاب سه‌زبانه

- کنترل کامل ترجمهٔ FA/EN/HR هر کتاب.
- Reader درون‌برنامه‌ای، اندازهٔ متن و حفظ محل مطالعه.
- عدم انتشار کتاب ناقص بدون علامت واضح وضعیت.
- دسترسی عمومی و خادمین به‌صورت جدا.

## موج ۳ — مدرسه و حساب‌ها

- الزام شنیدن کامل صوت درس.
- الزام ارسال تکلیف پیش از امتحان.
- راهنمای سه‌زبانه مدرسه و فرمول ۷۰٪ امتحان + ۳۰٪ تکالیف.
- اعتبارسنجی دقیق ثبت‌نام.
- بازیابی رمز فقط برای حساب کامل و تأییدشده.
- حذف کامل حساب و حذف فقط درخواست به‌صورت جدا.
- ابزار پاک‌سازی حساب‌های تستی.

## موج ۴ — Admin و دسترسی خادمین

- انتخاب کاربر کامل و تأییدشده از فهرست.
- Grant/Revoke برای هر کتاب یا جزوهٔ مشخص.
- مدیریت چندویدیویی برای رمز شخصی یک‌دستگاهه.
- تست کامل RBAC و جلوگیری از دسترسی Delegate به عملیات Owner.

## موج ۵ — Android Quality و Google Play

پیش از ساخت AAB بعدی:

- کنترل و ارتقای `targetSdk` به API 36 در صورت نیاز.
- فعال‌سازی R8 و Resource Shrinking در Release Build.
- بررسی مصرف RAM، Bitmap و حافظهٔ پس‌زمینه.
- Lazy Loading برای اپوکریفا، کتابخانه و فهرست موعظات.
- آزادسازی Object URL، DOM و داده‌های بزرگ هنگام خروج از صفحه.
- تعیین سقف دانلود آفلاین و ابزار پاک‌سازی واقعی.
- تست روی دستگاه کم‌حافظه و متوسط.
- بررسی Android Vitals در Play Console.

## موج ۶ — انتقال امن حساب به گوشی جدید

تا پیش از الزام Google Play در ۲۰۲۷:

- پیاده‌سازی Restore Credentials در Android Credential Manager.
- صدور Restore Key پس از ورود موفق.
- ثبت امن کلید در Backend.
- ایجاد Session تازه روی دستگاه جدید.
- حذف Restore Key هنگام Logout یا Delete Account.
- اتصال مجدد Device ID و اعلان‌ها روی دستگاه جدید.

## کنترل نهایی هر انتشار

- Backup و ثبت SHA آخرین نسخهٔ سالم.
- Compare شاخه با `main`.
- ممنوعیت حذف فایل‌های ناشناخته.
- بررسی Syntax و Inline Scripts.
- تست Auth، School، Audio، Offline، Video، Bible و Admin.
- افزایش Version و Cache Bust به‌صورت هماهنگ.
- انتشار GitHub Pages فقط پس از Validation.
- ساخت AAB / iOS package فقط بعد از تأیید نسخهٔ وب و QA.
