/* New Hope 7 Admin v3.1.1 — scroll-safe PDF/DOCX to in-app book conversion */
(()=>{'use strict';
const VERSION='3.1.1-library-clean';
const PDFJS_URL='https://cdn.jsdelivr.net/npm/pdfjs-dist@4.10.38/build/pdf.min.mjs';
const PDFJS_WORKER='https://cdn.jsdelivr.net/npm/pdfjs-dist@4.10.38/build/pdf.worker.min.mjs';
const MAMMOTH_URL='https://cdn.jsdelivr.net/npm/mammoth@1.12.0/mammoth.browser.min.js';
const L=(fa,en,hr)=>typeof lang!=='undefined'&&lang==='fa'?fa:typeof lang!=='undefined'&&lang==='hr'?hr:en;
const E=v=>typeof h==='function'?h(v):String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let pdfPromise=null,mammothPromise=null,currentRow=null,lastFile=null,busy=false,reader=fresh();
function fresh(){return{enabled:true,mode:'text',language:typeof lang!=='undefined'&&['fa','en','hr'].includes(lang)?lang:'fa',status:'none',progress:0,text:'',pages:[],all:{},message:'',pageCount:0}}
function clone(v){return JSON.parse(JSON.stringify(v))}
function parse(v){if(v&&typeof v==='object')return clone(v);try{return JSON.parse(String(v||'{}'))||{}}catch(_){return{}}}
function payload(all,language){const raw=all?.[language];if(typeof raw==='string')return{text:raw,pages:[]};if(raw&&typeof raw==='object')return{text:String(raw.text||''),pages:Array.isArray(raw.pages)?raw.pages:[]};return{text:'',pages:[]}}
function loadRow(row){currentRow=row||null;lastFile=null;if(!row){reader=fresh();return}const all=parse(row.reader_text),language=['fa','en','hr'].includes(row.reader_language)?row.reader_language:'fa',data=payload(all,language);reader={enabled:['text','both'].includes(String(row.reader_mode||''))||String(row.resource_type)==='apocrypha',mode:['text','both'].includes(String(row.reader_mode||''))?String(row.reader_mode):'text',language,status:String(row.reader_status||'none'),progress:String(row.reader_status)==='ready'?100:0,text:data.text,pages:data.pages,all,message:'',pageCount:Number(row.reader_page_count||data.pages.length||0)}}
function saveLanguage(){reader.all=Object.assign({},reader.all,{[reader.language]:{text:String(reader.text||''),pages:Array.isArray(reader.pages)?reader.pages:[]}})}
function paginate(text,max=5200){const paragraphs=String(text||'').split(/\n\s*\n/).map(v=>v.trim()).filter(Boolean),pages=[];let current='';for(const p of paragraphs){if(current&&current.length+p.length+2>max){pages.push({number:pages.length+1,text:current.trim()});current=''}current+=(current?'\n\n':'')+p}if(current.trim())pages.push({number:pages.length+1,text:current.trim()});return pages.length?pages:[{number:1,text:String(text||'').trim()}]}
function statusText(){if(busy)return reader.message||L('در حال تبدیل فایل…','Converting file…','Pretvaranje datoteke…');if(reader.status==='ready')return L(`کتاب آماده است: ${reader.pageCount||1} بخش. اکنون مورد را ذخیره کنید.`,`Book ready: ${reader.pageCount||1} sections. Save the item now.`,`Knjiga je spremna: ${reader.pageCount||1} dijelova.`);if(reader.status==='needs_ocr')return L('فایل اسکن‌شده است یا متن کافی ندارد. متن صحیح را در کادر وارد و ذخیره کنید.','The file is scanned or has no usable text. Paste corrected text and save.','Datoteka je skenirana. Unesite ispravan tekst.');if(reader.status==='error')return reader.message||L('تبدیل انجام نشد.','Conversion failed.','Pretvorba nije uspjela.');return L('پس از انتخاب PDF یا DOCX، متن به‌صورت خودکار استخراج می‌شود.','Text is extracted automatically after choosing a PDF or DOCX.','Tekst se automatski izdvaja nakon odabira datoteke.')}
function panel(){return`<section class="nh7-v311-reader" data-nh7-reader-v311><div class="nh7-v311-reader-head"><div><h3>📖 ${E(L('تبدیل فایل به کتاب خواندنی داخل اپ','Convert file to a readable in-app book','Pretvori datoteku u knjigu u aplikaciji'))}</h3><p class="muted small">${E(L('کاربر به‌جای بازکردن PDF، متن صفحه‌بندی‌شده را داخل خود اپ مطالعه می‌کند.','Instead of opening a PDF, the user reads paginated text inside the app.','Korisnik čita tekst unutar aplikacije.'))}</p></div><span class="nh7-v311-reader-badge">${E(L('بدون بازکردن فایل','No file viewer','Bez otvaranja datoteke'))}</span></div><div class="nh7-v311-grid3"><label>${E(L('فعال‌سازی','Enable','Omogući'))}<select data-v311-reader-enabled><option value="1" ${reader.enabled?'selected':''}>${E(L('فعال','Enabled','Omogućeno'))}</option><option value="0" ${!reader.enabled?'selected':''}>${E(L('غیرفعال','Disabled','Onemogućeno'))}</option></select></label><label>${E(L('روش نمایش','Display method','Način prikaza'))}<select data-v311-reader-mode ${reader.enabled?'':'disabled'}><option value="text" ${reader.mode==='text'?'selected':''}>${E(L('فقط کتاب داخل اپ','In-app book only','Samo knjiga u aplikaciji'))}</option><option value="both" ${reader.mode==='both'?'selected':''}>${E(L('کتاب داخل اپ + فایل اصلی','Book + original file','Knjiga + datoteka'))}</option></select></label><label>${E(L('زبان متن','Text language','Jezik teksta'))}<select data-v311-reader-language ${reader.enabled?'':'disabled'}><option value="fa" ${reader.language==='fa'?'selected':''}>فارسی</option><option value="en" ${reader.language==='en'?'selected':''}>English</option><option value="hr" ${reader.language==='hr'?'selected':''}>Hrvatski</option></select></label></div><div id="nh7-v311-reader-status" class="nh7-v311-status">${E(statusText())}</div><span class="nh7-v311-track"><i id="nh7-v311-reader-progress" style="width:${Math.max(0,Math.min(100,Number(reader.progress)||0))}%"></i></span><label>${E(L('متن استخراج‌شده — قبل از انتشار قابل اصلاح','Extracted text — editable before publishing','Izdvojeni tekst — moguće urediti'))}<textarea data-v311-reader-text ${reader.enabled?'':'disabled'} placeholder="${E(L('متن فایل اینجا نمایش داده می‌شود.','File text appears here.','Tekst datoteke prikazuje se ovdje.'))}">${E(reader.text)}</textarea></label><div class="nh7-v311-reader-actions"><button type="button" class="btn secondary" data-v311-extract-local ${lastFile&&!busy?'':'disabled'}>⟳ ${E(L('استخراج دوباره از فایل انتخاب‌شده','Extract selected file again','Ponovno izdvoji odabranu datoteku'))}</button><button type="button" class="btn secondary" data-v311-extract-existing ${currentRow?.storage_path&&!busy?'':'disabled'}>☁ ${E(L('تبدیل فایل موجود','Convert existing file','Pretvori postojeću datoteku'))}</button><button type="button" class="btn ghost" data-v311-reader-clear ${busy?'disabled':''}>× ${E(L('پاک‌کردن متن','Clear text','Očisti tekst'))}</button></div></section>`}
function sync(){const box=document.querySelector('[data-nh7-reader-v311]');if(!box)return;const status=document.getElementById('nh7-v311-reader-status'),bar=document.getElementById('nh7-v311-reader-progress'),text=box.querySelector('[data-v311-reader-text]');if(status)status.textContent=statusText();if(bar)bar.style.width=Math.max(0,Math.min(100,Number(reader.progress)||0))+'%';if(text&&text!==document.activeElement&&text.value!==reader.text)text.value=reader.text;const a=box.querySelector('[data-v311-extract-local]'),b=box.querySelector('[data-v311-extract-existing]');if(a)a.disabled=!lastFile||busy;if(b)b.disabled=!currentRow?.storage_path||busy}
async function pdfjs(){if(!pdfPromise)pdfPromise=import(PDFJS_URL).then(m=>{m.GlobalWorkerOptions.workerSrc=PDFJS_WORKER;return m});return pdfPromise}
async function mammoth(){if(window.mammoth)return window.mammoth;if(!mammothPromise)mammothPromise=new Promise((resolve,reject)=>{const s=document.createElement('script');s.src=MAMMOTH_URL;s.onload=()=>resolve(window.mammoth);s.onerror=()=>reject(new Error('Mammoth could not load'));document.head.appendChild(s)});return mammothPromise}
function pageText(content){const lines=[];let line='';for(const item of content.items||[]){const part=String(item?.str||'').trim();if(part){if(line&&!/^[،؛:,.!?؟)]/.test(part))line+=' ';line+=part}if(item?.hasEOL&&line.trim()){lines.push(line.trim());line=''}}if(line.trim())lines.push(line.trim());return lines.join('\n').replace(/\n{3,}/g,'\n\n')}
async function extractPdf(file){const lib=await pdfjs(),doc=await lib.getDocument({data:await file.arrayBuffer(),useWorkerFetch:true,isEvalSupported:false}).promise,pages=[];for(let i=1;i<=doc.numPages;i++){const page=await doc.getPage(i),content=await page.getTextContent({includeMarkedContent:true});pages.push({number:i,text:pageText(content)});reader.progress=Math.round(i/doc.numPages*92);reader.message=L(`در حال خواندن صفحه ${i} از ${doc.numPages}…`,`Reading page ${i} of ${doc.numPages}…`,`Čitanje stranice ${i} od ${doc.numPages}…`);sync();await new Promise(r=>setTimeout(r,0))}return pages}
async function extractDocx(file){const lib=await mammoth(),result=await lib.extractRawText({arrayBuffer:await file.arrayBuffer()}),text=String(result?.value||'').replace(/\r/g,'').replace(/\n{3,}/g,'\n\n').trim();return paginate(text)}
async function extract(file){if(!file||busy)return;lastFile=file;busy=true;reader.enabled=true;reader.status='none';reader.progress=3;reader.message=L('در حال بررسی فایل…','Inspecting file…','Provjera datoteke…');sync();try{let pages;if(/\.docx$/i.test(file.name)||String(file.type).includes('wordprocessingml'))pages=await extractDocx(file);else if(/\.pdf$/i.test(file.name)||file.type==='application/pdf')pages=await extractPdf(file);else throw new Error(L('فقط PDF یا DOCX قابل تبدیل است.','Only PDF or DOCX can be converted.','Moguće je pretvoriti samo PDF ili DOCX.'));const text=pages.map(p=>String(p.text||'').trim()).filter(Boolean).join('\n\n────────\n\n').trim(),meaningful=text.replace(/\s/g,'').length>=Math.max(80,pages.length*18);reader.pages=pages;reader.pageCount=pages.length;reader.text=text;reader.status=meaningful?'ready':'needs_ocr';reader.progress=100;reader.message='';saveLanguage()}catch(error){reader.status='error';reader.message=error?.message||String(error);reader.progress=0}finally{busy=false;sync()}}
async function fetchExisting(){if(!currentRow?.storage_path||busy)return;busy=true;reader.message=L('در حال دریافت فایل موجود…','Downloading existing file…','Dohvaćanje postojeće datoteke…');reader.progress=3;sync();try{const path=String(currentRow.storage_path).replace(/^\/+/, '').split('/').map(encodeURIComponent).join('/'),response=await fetch(`${SUPABASE_URL}/storage/v1/object/authenticated/nh7-library/${path}`,{headers:{apikey:SUPABASE_KEY,Authorization:'Bearer '+token},cache:'no-store'});if(!response.ok)throw new Error(await response.text());const blob=await response.blob(),file=new File([blob],currentRow.file_name||'book.pdf',{type:currentRow.mime_type||blob.type||'application/pdf'});busy=false;await extract(file)}catch(error){busy=false;reader.status='error';reader.message=error?.message||String(error);reader.progress=0;sync()}}
if(typeof renderActivePanel==='function'){const old=renderActivePanel;renderActivePanel=function(){const html=old();return activeTab==='library'?html+panel():html}}
if(typeof adminRpc==='function'&&!adminRpc.__nh7LibraryCleanV311){const old=adminRpc;const wrapped=async function(name,data={}){let next=data;if(name==='nh7_admin_library_save_item_v224'&&data?.p_item){saveLanguage();const text=String(reader.text||'').trim(),status=reader.enabled?(text?'ready':reader.status==='needs_ocr'?'needs_ocr':'none'):'none';next=Object.assign({},data,{p_item:Object.assign({},data.p_item,{reader_mode:reader.enabled?(reader.mode||'text'):'file',reader_language:reader.language,reader_text:reader.enabled?reader.all:{},reader_status:status,reader_page_count:reader.enabled?reader.pageCount:0})})}return old(name,next)};wrapped.__nh7LibraryCleanV311=true;adminRpc=wrapped}
if(typeof nh7EditLibraryV224==='function'){const old=nh7EditLibraryV224;nh7EditLibraryV224=function(id){const row=(state.libraryV224?.items||[]).find(x=>String(x.id)===String(id));loadRow(row);return old(id)}}
if(typeof nh7ResetLibraryDraftV224==='function'){const old=nh7ResetLibraryDraftV224;nh7ResetLibraryDraftV224=function(){loadRow(null);return old()}}
document.addEventListener('change',event=>{const file=event.target.closest?.('#nh7_library_editor_v224 input[type="file"]');if(file?.files?.[0]){lastFile=file.files[0];extract(lastFile);return}const enabled=event.target.closest?.('[data-v311-reader-enabled]');if(enabled){reader.enabled=enabled.value==='1';sync();return}const mode=event.target.closest?.('[data-v311-reader-mode]');if(mode){reader.mode=mode.value;return}const language=event.target.closest?.('[data-v311-reader-language]');if(language){saveLanguage();reader.language=language.value;const data=payload(reader.all,reader.language);reader.text=data.text;reader.pages=data.pages;reader.pageCount=data.pages.length;reader.status=reader.text.trim()?'ready':'none';sync()}},true);
document.addEventListener('input',event=>{const text=event.target.closest?.('[data-v311-reader-text]');if(!text)return;reader.text=text.value;reader.pages=paginate(reader.text);reader.pageCount=reader.pages.length;reader.status=reader.text.trim()?'ready':'none';saveLanguage();sync()},true);
document.addEventListener('click',event=>{if(event.target.closest?.('[data-v311-extract-local]')){event.preventDefault();extract(lastFile);return}if(event.target.closest?.('[data-v311-extract-existing]')){event.preventDefault();fetchExisting();return}if(event.target.closest?.('[data-v311-reader-clear]')){event.preventDefault();reader.text='';reader.pages=[];reader.pageCount=0;reader.status='none';reader.progress=0;saveLanguage();sync()}},true);
window.NH7_ADMIN_LIBRARY_CLEAN_VERSION=VERSION;
})();
