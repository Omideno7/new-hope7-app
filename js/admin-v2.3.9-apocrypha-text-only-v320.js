/* New Hope 7 Admin — force Apocrypha publications to text-only reader mode v3.2.0 */
(()=>{'use strict';
const VERSION='3.2.0-apocrypha-text-only';
let installed=false;
function mount(){if(typeof activeTab==='undefined'||activeTab!=='library')return;const editor=document.getElementById('nh7_library_editor_v224');if(!editor)return;const type=editor.querySelector('select[onchange*="resource_type"]'),mode=editor.querySelector('[data-v311-reader-mode]'),enabled=editor.querySelector('[data-v311-reader-enabled]');if(type?.value!=='apocrypha')return;if(enabled){enabled.value='1';enabled.disabled=true}if(mode){mode.value='text';mode.disabled=true}let note=editor.querySelector('[data-nh7-apocrypha-text-only-v320]');if(!note){note=document.createElement('div');note.dataset.nh7ApocryphaTextOnlyV320='1';note.className='notice';note.textContent=typeof lang!=='undefined'&&lang==='fa'?'اپوکریفا فقط به‌صورت متن خواندنی داخل اپ منتشر می‌شود؛ PDF به کاربر نمایش داده نمی‌شود.':typeof lang!=='undefined'&&lang==='hr'?'Apokrifi se objavljuju samo kao čitljiv tekst u aplikaciji; PDF nije vidljiv korisniku.':'Apocrypha is published only as readable in-app text; the PDF is not shown to users.';mode?.closest('label')?.insertAdjacentElement('afterend',note)}}
function install(){if(installed||typeof adminRpc!=='function'||typeof render!=='function')return false;installed=true;const oldRpc=adminRpc;adminRpc=window.adminRpc=async function(name,data={}){if(name==='nh7_admin_library_save_item_v224'&&data?.p_item?.resource_type==='apocrypha'){data=Object.assign({},data,{p_item:Object.assign({},data.p_item,{reader_mode:'text'})})}return oldRpc(name,data)};const oldRender=render;render=window.render=function(...args){const out=oldRender.apply(this,args);requestAnimationFrame(mount);return out};window.NH7_ADMIN_APOCRYPHA_TEXT_ONLY_VERSION=VERSION;requestAnimationFrame(mount);return true}
let tries=0;const boot=()=>{tries++;if(install())return;if(tries<30)setTimeout(boot,50)};boot();
})();
