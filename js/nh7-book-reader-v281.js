/* New Hope 7 v2.8.1 — stable Bible-like reader for books and Apocrypha */
(()=>{'use strict';
const VERSION='2.8.1-book-reader';
const URL='https://gpzcwffxnddhaeaogdyo.supabase.co';
const KEY='sb_publishable_v3xXEaJ5Fml7-te1mI4-0g_7R86oM37';
const SESSION_KEY='nh7_user_session_v170';
const CODE_KEY='nh7_minister_library_code';
const POS_PREFIX='nh7_book_position_';
let catalog=new Map(),catalogBusy=false,lastCatalog=0,modal=null,book=null,pageIndex=0,searchTimer=0;
const lang=()=>{const value=localStorage.getItem('nh7_lang')||document.documentElement.lang||'en';return ['fa','en','hr'].includes(value)?value:'en'};
const L=(fa,en,hr)=>lang()==='fa'?fa:lang()==='hr'?hr:en;
const E=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
function session(){try{return JSON.parse(localStorage.getItem(SESSION_KEY)||'null')}catch(_){return null}}
function accessToken(){return String(session()?.access_token||'')}
function userEmail(){return String(session()?.user?.email||localStorage.getItem('nh7_manual_email')||'').trim().toLowerCase()}
function deviceId(){let value=localStorage.getItem('nh7_device_id');if(!value){value='dev_'+(crypto.randomUUID?.()||Date.now()+'_'+Math.random().toString(36).slice(2));localStorage.setItem('nh7_device_id',value)}return value}
function headers(){return{apikey:KEY,Authorization:'Bearer '+(accessToken()||KEY),'Content-Type':'application/json'}}
function titleOf(row){const l=lang();return row?.['title_'+l]||row?.title_en||row?.title_fa||row?.title_hr||row?.file_name||L('کتاب','Book','Knjiga')}
async function loadCatalog(force=false){if(catalogBusy||!accessToken()||(!force&&Date.now()-lastCatalog<15000))return;catalogBusy=true;try{const response=await fetch(`${URL}/rest/v1/nh7_library_items_v224?select=id,title_fa,title_en,title_hr,audience,resource_type,apocrypha_book,reader_mode,reader_language,reader_status,reader_available,reader_page_count&reader_available=eq.true`,{headers:headers(),cache:'no-store'});if(!response.ok)throw new Error(await response.text());const rows=await response.json();catalog=new Map((Array.isArray(rows)?rows:[]).map(row=>[String(row.id),row]));lastCatalog=Date.now()}catch(error){console.warn('In-app book catalog',error)}finally{catalogBusy=false;decorate()}}
function decorate(){document.querySelectorAll('[data-library-open]').forEach(fileButton=>{const id=String(fileButton.dataset.libraryOpen||''),row=catalog.get(id);if(!row?.reader_available)return;const actions=fileButton.parentElement;if(!actions)return;let readButton=actions.querySelector(`[data-nh7-book-open="${CSS.escape(id)}"]`);if(!readButton){readButton=document.createElement('button');readButton.type='button';readButton.className='primary-btn nh7-book-open-btn';readButton.dataset.nh7BookOpen=id;readButton.textContent='📖 '+L('مطالعه کتاب داخل اپ','Read book in app','Čitaj knjigu u aplikaciji');actions.insertBefore(readButton,fileButton)}if(row.reader_mode==='text')fileButton.style.display='none';else{fileButton.style.display='';fileButton.textContent='📄 '+L('فایل اصلی','Original file','Izvorna datoteka');fileButton.classList.remove('primary-btn');fileButton.classList.add('secondary-btn')}})}
function savedMinisterCode(){try{const data=JSON.parse(sessionStorage.getItem(CODE_KEY)||'null');return data&&Date.now()-Number(data.at||0)<12*60*60*1000?String(data.code||''):''}catch(_){return''}}
async function readerRpc(item,code=''){const response=await fetch(`${URL}/rest/v1/rpc/nh7_library_reader_access_v250`,{method:'POST',headers:headers(),body:JSON.stringify({p_item_id:item.id,p_code:code,p_device_id:deviceId(),p_user_email:userEmail()}),cache:'no-store'});const text=await response.text();let data={};try{data=text?JSON.parse(text):{}}catch(_){data={message:text}}if(!response.ok)throw new Error(data.message||text||response.statusText);return Array.isArray(data)?data[0]:data}
function accessError(data){const code=String(data?.code||'');if(code==='school_approval_required')return L('دسترسی مدرسه شما هنوز تأیید نشده است.','Your school access is not approved yet.','Pristup školi još nije odobren.');if(['code_required','invalid_code','expired','max_uses'].includes(code))return L('کد کتابخانه خادمان نامعتبر یا منقضی است.','The ministers library code is invalid or expired.','Kod knjižnice nije valjan ili je istekao.');if(code==='reader_not_ready')return L('نسخهٔ کتاب داخل اپ هنوز آماده نشده است.','The in-app book is not ready yet.','Knjiga u aplikaciji još nije spremna.');return data?.message||L('کتاب باز نشد.','The book could not be opened.','Knjiga se nije mogla otvoriti.')}
function readerPayload(data){const raw=data?.reader;if(typeof raw==='string')return{text:raw,pages:[]};if(raw&&typeof raw==='object')return{text:String(raw.text||''),pages:Array.isArray(raw.pages)?raw.pages:[]};return{text:'',pages:[]}}
function makePages(payload){if(payload.pages.length)return payload.pages.map((page,index)=>({number:Number(page.number||index+1),text:String(page.text||'')}));const paragraphs=String(payload.text||'').split(/\n\s*\n/).map(value=>value.trim()).filter(Boolean),pages=[];let current='';for(const paragraph of paragraphs){if(current&&current.length+paragraph.length+2>5200){pages.push({number:pages.length+1,text:current.trim()});current=''}current+=(current?'\n\n':'')+paragraph}if(current.trim())pages.push({number:pages.length+1,text:current.trim()});return pages.length?pages:[{number:1,text:String(payload.text||'').trim()}]}
function readPosition(id){try{return JSON.parse(localStorage.getItem(POS_PREFIX+id)||'{}')}catch(_){return{}}}
function savePosition(){if(!book)return;localStorage.setItem(POS_PREFIX+book.item.id,JSON.stringify({page:pageIndex,scroll:modal?.querySelector('.nh7-book-article')?.scrollTop||0,at:Date.now()}))}
async function openBook(id){const item=catalog.get(String(id));if(!item)return;if(!accessToken()){alert(L('برای مطالعه کتاب ابتدا وارد حساب شوید.','Sign in before reading.','Prijavite se prije čitanja.'));return}let code=item.audience==='ministers'?savedMinisterCode():'';let data=await readerRpc(item,code).catch(error=>({allowed:false,code:'request_failed',message:error.message}));if(!data?.allowed&&item.audience==='ministers'&&['code_required','invalid_code','expired','max_uses'].includes(String(data?.code||''))){code=String(prompt(L('کد کتابخانه خادمان را وارد کنید:','Enter the ministers library code:','Unesite kod knjižnice:'),'')||'').trim();if(!code)return;data=await readerRpc(item,code).catch(error=>({allowed:false,code:'request_failed',message:error.message}));if(data?.allowed)sessionStorage.setItem(CODE_KEY,JSON.stringify({code,at:Date.now()}))}if(!data?.allowed){alert(accessError(data));return}const pages=makePages(readerPayload(data)),position=readPosition(item.id);book={item,data,pages,language:data.reader_language||lang()};pageIndex=Math.max(0,Math.min(pages.length-1,Number(position.page||0)));showReader(Number(position.scroll||0))}
function removeModal(preserveBook=false){if(modal){savePosition();modal.remove();modal=null}document.body.classList.remove('nh7-book-open');if(!preserveBook)book=null}
function closeReader(){removeModal(false)}
function shell(){const title=book.data['title_'+book.language]||titleOf(book.item);return `<section class="nh7-book-shell"><header class="nh7-book-head"><button type="button" data-book-close>‹ ${E(L('بازگشت','Back','Natrag'))}</button><div><h2>${E(title)}</h2><small>${E(L('کتاب داخل اپ','In-app book','Knjiga u aplikaciji'))} · ${book.pages.length} ${E(L('بخش','sections','dijelova'))}</small></div><button type="button" data-book-top>↑</button></header><div class="nh7-book-toolbar"><input type="search" data-book-search placeholder="${E(L('جست‌وجو در کتاب…','Search in book…','Pretraži knjigu…'))}"><button type="button" class="nh7-book-tool-btn" data-book-font-down>A−</button><button type="button" class="nh7-book-tool-btn" data-book-font-up>A+</button><select data-book-theme><option value="light">${E(L('روشن','Light','Svijetlo'))}</option><option value="sepia">${E(L('کاغذی','Sepia','Sepija'))}</option><option value="dark">${E(L('تیره','Dark','Tamno'))}</option></select></div><div class="nh7-book-search-results" data-book-results></div><div class="nh7-book-progress"><i data-book-progress></i></div><main class="nh7-book-main"><article class="nh7-book-article"></article><aside class="nh7-book-sidebar"><h3>${E(L('فهرست بخش‌ها','Sections','Dijelovi'))}</h3><div class="nh7-book-page-list"></div></aside></main><footer class="nh7-book-footer"><button type="button" class="nh7-book-prev" data-book-prev>‹ ${E(L('بخش قبلی','Previous','Prethodno'))}</button><span class="nh7-book-count" data-book-count></span><button type="button" class="nh7-book-next" data-book-next>${E(L('بخش بعدی','Next','Sljedeće'))} ›</button></footer></section>`}
function showReader(savedScroll=0){if(!book)return;removeModal(true);modal=document.createElement('div');modal.className='nh7-book-modal';modal.dir=book.language==='fa'?'rtl':'ltr';modal.dataset.theme=localStorage.getItem('nh7_book_theme')||'light';modal.innerHTML=shell();document.body.appendChild(modal);document.body.classList.add('nh7-book-open');bindReader();renderPage(pageIndex,'',savedScroll)}
function highlight(text,query){const safe=E(text);if(!query)return safe;const escaped=query.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');try{return safe.replace(new RegExp(escaped,'gi'),match=>`<mark>${match}</mark>`)}catch(_){return safe}}
function renderPage(index,query='',savedScroll=0){if(!book||!modal)return;savePosition();pageIndex=Math.max(0,Math.min(book.pages.length-1,index));const page=book.pages[pageIndex],article=modal.querySelector('.nh7-book-article'),paragraphs=String(page.text||'').split(/\n\s*\n/).filter(Boolean);article.innerHTML=`<section class="nh7-book-page"><h3>${E(L('بخش','Section','Dio'))} ${pageIndex+1}</h3>${paragraphs.length?paragraphs.map(paragraph=>`<p>${highlight(paragraph,query).replace(/\n/g,'<br>')}</p>`).join(''):`<div>${highlight(page.text||'',query).replace(/\n/g,'<br>')}</div>`}</section>`;article.style.fontSize=(Number(localStorage.getItem('nh7_book_font')||20))+'px';modal.querySelector('[data-book-count]').textContent=`${pageIndex+1} / ${book.pages.length}`;modal.querySelector('[data-book-progress]').style.width=((pageIndex+1)/book.pages.length*100)+'%';modal.querySelector('[data-book-prev]').disabled=pageIndex===0;modal.querySelector('[data-book-next]').disabled=pageIndex===book.pages.length-1;modal.querySelector('.nh7-book-page-list').innerHTML=book.pages.map((_,i)=>`<button type="button" data-book-page="${i}" class="${i===pageIndex?'active':''}">${E(L('بخش','Section','Dio'))} ${i+1}</button>`).join('');requestAnimationFrame(()=>{article.scrollTop=savedScroll||0;savePosition()})}
function searchBook(query){query=String(query||'').trim();const results=modal?.querySelector('[data-book-results]');if(!results)return;if(!query){results.classList.remove('show');results.textContent='';renderPage(pageIndex);return}const matches=[];book.pages.forEach((page,index)=>{const text=String(page.text||''),position=text.toLocaleLowerCase().indexOf(query.toLocaleLowerCase());if(position>=0)matches.push({index,snippet:text.slice(Math.max(0,position-45),position+query.length+80).replace(/\s+/g,' ')})});results.classList.add('show');results.innerHTML=matches.length?matches.slice(0,25).map(match=>`<button type="button" data-book-search-page="${match.index}">${E(L('بخش','Section','Dio'))} ${match.index+1}: ${highlight(match.snippet,query)}</button>`).join(''):E(L('نتیجه‌ای پیدا نشد.','No matches found.','Nema rezultata.'));renderPage(pageIndex,query)}
function changeFont(delta){const current=Number(localStorage.getItem('nh7_book_font')||20),next=Math.max(15,Math.min(34,current+delta));localStorage.setItem('nh7_book_font',String(next));const article=modal?.querySelector('.nh7-book-article');if(article)article.style.fontSize=next+'px'}
function bindReader(){modal.querySelector('[data-book-theme]').value=modal.dataset.theme;modal.querySelector('[data-book-theme]').onchange=event=>{modal.dataset.theme=event.target.value;localStorage.setItem('nh7_book_theme',event.target.value)};modal.querySelector('[data-book-search]').oninput=event=>{clearTimeout(searchTimer);searchTimer=setTimeout(()=>searchBook(event.target.value),180)};modal.addEventListener('click',event=>{if(event.target.closest('[data-book-close]'))return closeReader();if(event.target.closest('[data-book-top]'))return modal.querySelector('.nh7-book-article')?.scrollTo({top:0,behavior:'smooth'});if(event.target.closest('[data-book-prev]'))return renderPage(pageIndex-1);if(event.target.closest('[data-book-next]'))return renderPage(pageIndex+1);if(event.target.closest('[data-book-font-up]'))return changeFont(1);if(event.target.closest('[data-book-font-down]'))return changeFont(-1);const pageButton=event.target.closest('[data-book-page]');if(pageButton)return renderPage(Number(pageButton.dataset.bookPage));const resultButton=event.target.closest('[data-book-search-page]');if(resultButton)return renderPage(Number(resultButton.dataset.bookSearchPage),modal.querySelector('[data-book-search]').value)});modal.querySelector('.nh7-book-article').addEventListener('scroll',()=>{clearTimeout(modal._saveTimer);modal._saveTimer=setTimeout(savePosition,150)},{passive:true})}
document.addEventListener('click',event=>{const button=event.target.closest?.('[data-nh7-book-open]');if(!button)return;event.preventDefault();event.stopImmediatePropagation();openBook(button.dataset.nh7BookOpen)},true);
document.addEventListener('keydown',event=>{if(event.key==='Escape'&&modal)closeReader()},true);
window.addEventListener('beforeunload',savePosition);
const observer=new MutationObserver(()=>{if(document.querySelector('[data-library-open]'))loadCatalog();decorate()});observer.observe(document.documentElement,{childList:true,subtree:true});
setInterval(()=>{if(document.querySelector('[data-library-open]'))loadCatalog();decorate()},4000);setTimeout(()=>loadCatalog(true),700);
window.NH7_BOOK_READER_VERSION=VERSION;
})();
