name: Store Review v469 Preview QA
on: workflow_dispatch
permissions:
  contents: read
jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Old Settings-card preview is superseded. More-menu QA is being prepared; main is untouched.'
