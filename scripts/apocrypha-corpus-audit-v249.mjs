import fs from 'node:fs'; import path from 'node:path';
const root=process.cwd(), r=p=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8')), e=p=>fs.existsSync(path.join(root,p)), K=(b,c,v)=>`${b}|${+c}|${+v}`;
const blockers=[],warnings=[]; const B=(code,detail)=>blockers.push({code,detail}), W=(code,detail)=>warnings.push({code,detail});
const progress=r('data/apocrypha/review/progress-v245.json'), expected=new Map(progress.complete_books.map(x=>[x.book_id,x]));
const canonical=new Map(); let sourceRows=0;
for(const [id,x] of expected){const p=`data/apocrypha/sources/en/${id}.en.json`;if(!e(p)){B('SOURCE_MISSING',p);continue}const s=r(p);let n=0;if(s.book_id!==id)B('SOURCE_ID',id);if(!s.source_translation||!s.source_license||!s.versification)B('SOURCE_METADATA',id);for(let ci=0;ci<s.chapters.length;ci++){const ch=s.chapters[ci];if(+ch.chapter!==ci+1)B('SOURCE_CHAPTER_ORDER',`${id}:${ch.chapter}`);let prev=null;for(const v of ch.verses){const ref=K(id,ch.chapter,v.verse);if(prev!==null&&+v.verse!==prev+1)B('SOURCE_VERSE_GAP',`${id} ${ch.chapter}: ${prev}->${v.verse}`);prev=+v.verse;if(!String(v.text_en||'').trim())B('SOURCE_BLANK_EN',ref);if(canonical.has(ref))B('SOURCE_DUPLICATE',ref);canonical.set(ref,{id,c:+ch.chapter,v:+v.verse,text:v.text_en});n++}}if(n!==+s.verse_count||n!==+x.verses)B('SOURCE_TOTAL',`${id}: ${n}/${s.verse_count}/${x.verses}`);sourceRows+=n}
if(expected.size!==19||sourceRows!==7501||canonical.size!==7501)B('CORPUS_BASELINE',`books=${expected.size} rows=${sourceRows} unique=${canonical.size}`);
const runtime=r('data/apocrypha/runtime/apocrypha-browser-19.preview.json'), idx=new Map();let runtimeRows=0;
for(const book of runtime.books||[])for(const ch of book.chapters||[])for(const v of ch.verses||[]){const ref=K(book.book_id,ch.chapter,v.verse);idx.set(ref,v);runtimeRows++;const s=canonical.get(ref);if(!s)B('RUNTIME_NON_SOURCE',ref);else if(v.text_en!==s.text)B('RUNTIME_EN_MISMATCH',ref);if(v.status_fa!=='in_review'||!String(v.text_fa||'').trim()){v.text_fa=null;v.status_fa=null}if(v.status_hr!=='in_review'||!String(v.text_hr||'').trim()){v.text_hr=null;v.status_hr=null}}
if((runtime.books||[]).length!==19||runtimeRows!==7501||idx.size!==7501)B('RUNTIME_TOTAL',`books=${runtime.books?.length} rows=${runtimeRows} unique=${idx.size}`);for(const ref of canonical.keys())if(!idx.has(ref))B('RUNTIME_MISSING',ref);
const regs=['data/apocrypha/review/translation-overlays-v245.json','data/apocrypha/review/translation-overlays-v246-continuation.json','data/apocrypha/review/translation-overlays-v247-audit-corrections.json'];const applied=new Map(),regStats=[];
function put(id,c,v,loc,text,status,file,ri){if(status!=='in_review'||!String(text||'').trim())return;const ref=K(id,c,v),t=idx.get(ref);if(!t){B('OVERLAY_NON_SOURCE',`${file}: ${ref}`);return}const lk=`${ref}|${loc}`;if(applied.has(lk)&&ri<2)W('OVERLAY_OVERRIDE',`${lk}: ${applied.get(lk)} -> ${file}`);t[`text_${loc}`]=text;t[`status_${loc}`]='in_review';applied.set(lk,file)}
function merge(d,file,ri){if(!d?.book_id||!expected.has(d.book_id)){B('OVERLAY_BOOK',file);return}const declared=['fa','hr'].includes(String(d.language||''))?String(d.language):'';if(Array.isArray(d.verses)){for(const row of d.verses){if(declared)put(d.book_id,d.chapter||1,row.verse,declared,row.text,'in_review',file,ri);else{put(d.book_id,d.chapter||1,row.verse,'fa',row.text_fa,row.status_fa,file,ri);put(d.book_id,d.chapter||1,row.verse,'hr',row.text_hr,row.status_hr,file,ri)}}}if(Array.isArray(d.chapters)){for(const ch of d.chapters)for(const row of ch.verses||[]){if(declared)put(d.book_id,ch.chapter,row.verse,declared,row.text,'in_review',file,ri);else{put(d.book_id,ch.chapter,row.verse,'fa',row.text_fa,row.status_fa,file,ri);put(d.book_id,ch.chapter,row.verse,'hr',row.text_hr,row.status_hr,file,ri)}}}}
for(let ri=0;ri<regs.length;ri++){const rp=regs[ri];if(!e(rp)){B('REGISTRY_MISSING',rp);continue}const reg=r(rp);let loaded=0;const seen=new Set();for(const file of reg.files||[]){if(seen.has(file)){B('REGISTRY_DUPLICATE_PATH',`${rp}:${file}`);continue}seen.add(file);if(!e(file)){B('OVERLAY_FILE_MISSING',file);continue}try{merge(r(file),file,ri);loaded++}catch(err){B('OVERLAY_JSON',`${file}: ${err.message}`)}}regStats.push({path:rp,declared:(reg.files||[]).length,loaded})}
const enoch=idx.get(K('1_enoch',108,15));if(enoch){enoch.text_en=String(enoch.text_en||'').replace(/\s*Printed in Great Britain by Richard Clay and Company, Ltd\., Bungay, Suffolk\.?\s*$/i,'').trim();enoch.text_hr=String(enoch.text_hr||'').replace(/\s*Tiskano u Velikoj Britaniji u tiskari Richard Clay and Company, Ltd\., Bungay, Suffolk\.?\s*$/i,'').trim();enoch.text_fa=String(enoch.text_fa||'').replace(/\s*چاپ‌شده در بریتانیای کبیر توسط Richard Clay and Company, Ltd\.، Bungay، Suffolk\.?\s*$/,'').trim()}
const az=idx.get(K('prayer_of_azariah',1,55));if(az)az.text_en='O ye fountains, bless ye the Lord: praise and exalt him above all for ever.';
const ph=/(TODO|TBD|TRANSLATE|\[\[(?:C\d+V\d+|V\d+)\]\]|\/n\/n|\\n\\n|\[object Object\])/iu, ctl=/[\u0000-\u0008\u000B\u000C\u000E-\u001F\u007F]/u, imp=/(Printed in Great Britain|Richard Clay and Company|Tiskano u Velikoj Britaniji|چاپ‌شده در بریتانیای کبیر)/iu, leg=/(Razgah|رازگاه|prepared[-_ ]existing|legacy[_ -]text)/iu,cyr=/[А-Яа-яЁё]/u;
let EN=0,FA=0,HR=0;const byBook={},missing={};for(const [ref,s] of canonical){const v=idx.get(ref);if(!v)continue;byBook[s.id]||={en:0,fa:0,hr:0,text_issues:0};missing[s.id]||={fa:[],hr:[]};const b=byBook[s.id],en=String(v.text_en||'').trim(),fa=String(v.text_fa||'').trim(),hr=String(v.text_hr||'').trim();if(en){EN++;b.en++}else B('FINAL_BLANK_EN',ref);if(fa&&v.status_fa==='in_review'){FA++;b.fa++}else{B('FINAL_MISSING_FA',ref);missing[s.id].fa.push(`${s.c}:${s.v}`)}if(hr&&v.status_hr==='in_review'){HR++;b.hr++}else{B('FINAL_MISSING_HR',ref);missing[s.id].hr.push(`${s.c}:${s.v}`)}for(const [loc,t] of [['fa',fa],['hr',hr]])if(t){if(ph.test(t)){B('TEXT_PLACEHOLDER',`${ref}|${loc}`);b.text_issues++}if(ctl.test(t)){B('TEXT_CONTROL',`${ref}|${loc}`);b.text_issues++}if(imp.test(t)){B('TEXT_IMPRINT',`${ref}|${loc}`);b.text_issues++}if(leg.test(t)){B('TEXT_LEGACY',`${ref}|${loc}`);b.text_issues++}if(loc==='hr'&&cyr.test(t)){B('HR_CYRILLIC',ref);b.text_issues++}}}
if(EN!==7501||FA!==7501||HR!==7501)B('FINAL_TOTAL',`EN=${EN} FA=${FA} HR=${HR}`);for(const [id,x] of expected){const b=byBook[id]||{};if(b.en!==x.verses||b.fa!==x.fa||b.hr!==x.hr)B('FINAL_BOOK_TOTAL',`${id}: ${b.en||0}/${b.fa||0}/${b.hr||0} expected ${x.verses}/${x.fa}/${x.hr}`)}
const t1411=idx.get(K('tobit',14,11)),t12=idx.get(K('tobit',1,2));if(!t1411?.text_fa?.includes('صد و پنجاه‌وهشت'))B('KNOWN_TOBIT_14_11','inactive');if(/قادش/u.test(t12?.text_fa||'')||/Kadeš/iu.test(t12?.text_hr||''))B('KNOWN_TOBIT_1_2','inactive');if(!/چشمه/u.test(az?.text_fa||'')||!/Izvori/iu.test(az?.text_hr||'')||!/fountains/iu.test(az?.text_en||''))B('KNOWN_AZARIAH_1_55','inactive');if(imp.test(`${enoch?.text_en||''} ${enoch?.text_fa||''} ${enoch?.text_hr||''}`))B('KNOWN_ENOCH_108_15','inactive');
const summary={};for(const x of blockers)summary[x.code]=(summary[x.code]||0)+1;const report={schema_version:1,audit_version:'2.4.9',generated_at:new Date().toISOString(),branch:process.env.GITHUB_REF_NAME||null,commit:process.env.GITHUB_SHA||null,coverage:{english:EN,persian:FA,croatian:HR},source_rows:sourceRows,registries:regStats,by_book:byBook,missing_refs:missing,blocker_summary:summary,blocker_count:blockers.length,blockers,warning_count:warnings.length,warnings,structural_release_gate_passed:blockers.length===0};fs.mkdirSync(path.join(root,'artifacts'),{recursive:true});fs.writeFileSync(path.join(root,'artifacts/apocrypha-corpus-audit-v249.json'),JSON.stringify(report,null,2)+'\n');console.log(`AUDIT249 EN=${EN} FA=${FA} HR=${HR} BLOCKERS=${blockers.length} WARNINGS=${warnings.length}`);console.log(JSON.stringify(summary));for(const [id,m] of Object.entries(missing))if(m.fa.length||m.hr.length)console.log(`MISSING ${id} FA=${m.fa.join(',')} HR=${m.hr.join(',')}`);for(const x of blockers.filter(x=>!x.code.startsWith('FINAL_MISSING')).slice(0,80))console.log(`BLOCKER ${x.code}: ${x.detail}`);process.exitCode=0;
